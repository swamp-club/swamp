vaults:
  local-test-vault:
    type: local_encryption
    config:
      auto_generate: true

  aws-test-vault:
    type: aws-secrets-manager
    config:
      region: us-east-1
      # Uses default AWS credential chain (environment vars, IAM roles, etc.)
